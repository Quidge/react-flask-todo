import React from 'react';

export default class NewTaskBar extends React.Component {
  // static propTypes = {
  //   name: React.PropTypes.string,
  // };

  render() {
    return (
      <div style={{width: `100%`, backgroundColor: "darkslategrey", color: "white"}}>I'm a task bar.</div>
    );
  }
}
